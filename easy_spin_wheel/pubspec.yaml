name: easy_spin_wheel
description: Easy spin wheel component with customizable rewards, animations and confetti effects
version: 1.0.0
homepage: https://github.com/your-org/easy_spin_wheel

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_fortune_wheel: ^1.3.2
  confetti: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
