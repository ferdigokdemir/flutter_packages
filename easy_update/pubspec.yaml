name: easy_update
description: "Remote Config based Flutter app version control package. Provides ready-to-use dialogs and services for mandatory and optional updates with 47 language support."
version: 1.0.0
homepage: https://github.com/ferdigokdemir/flutter_packages/tree/main/easy_update
repository: https://github.com/ferdigokdemir/flutter_packages
issue_tracker: https://github.com/ferdigokdemir/flutter_packages/issues
topics:
  - update
  - version
  - remote-config
  - dialog
  - localization

environment:
  sdk: ^3.8.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^9.0.0
  url_launcher: ^6.3.1
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
