name: launch_trigger
description: "Uygulama acilis/erteleme sayaclarina gore tetikleyici kontrol."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:


